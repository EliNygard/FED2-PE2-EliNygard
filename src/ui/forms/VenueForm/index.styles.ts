import styled from "styled-components";

export const StyledVenueForm = styled.form`
  margin-top: 2rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;

  p {
    font-size: 14px;
  }
`;

export const StyledImageItem = styled.div`
  display: grid;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
`;

export const StyledFieldset = styled.fieldset`
  display: grid;
  gap: 0.5rem;
`;
